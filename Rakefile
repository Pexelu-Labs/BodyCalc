# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'
require 'bubble-wrap'
require 'formotion'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'BodyCalc'
  app.delegate_class = 'BodyCalcApp'
  app.identifier = 'com.revLife.BodyCalcApp'
  app.icons << "icon.png"
end
